query Earnings(
  $timeFrame: TimeQuery!
  $startDate: DateTime!
  $endDate: DateTime!
) {
  getStatsNew(timeframe: $timeFrame, startDate: $startDate, endDate: $endDate) {
    currency
    lastOrderEarnings
    dataset {
      earning
      count
      time
      distance
      name
    }
  }
}
