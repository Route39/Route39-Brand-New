VITE_API_URL=http://localhost:3004/graphql
VITE_WS_URL=ws://localhost:3004/graphql
# Google Maps key is fetched from the admin-api at runtime via
# currentConfiguration.adminPanelAPIKey (editable in /management/settings).
# Production cutover only — set to "/admin/" before building behind the legacy reverse proxy.
VITE_BASE_PATH=/
